{
  "name": "crypto-arena",
  "version": "1.2.0",
  "description": "CryptoArena v1.2 — Hardhat + Grafana + Sentiment + Regime v2",
  "scripts": {
    "compile": "hardhat compile",
    "test:contracts": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:sepolia": "hardhat run scripts/hardhat_deploy.js --network base-sepolia",
    "deploy:base": "hardhat run scripts/hardhat_deploy.js --network base",
    "verify:sepolia": "hardhat run scripts/hardhat_verify.js --network base-sepolia",
    "size-contracts": "hardhat size-contracts",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "chai": "^4.3.10",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "hardhat": "^2.19.0",
    "hardhat-contract-sizer": "^2.10.0",
    "hardhat-gas-reporter": "^1.0.10",
    "solidity-coverage": "^0.8.5"
  }
}
